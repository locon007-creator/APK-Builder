name: APK Builder 5.2.0 audited host build

on:
  push:
    branches:
      - apkbuilder-permanent-reset
    paths:
      - .github/BUILD_PERMANENT_RESET_ONCE

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout isolated build branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Test update source selection flow
        run: python3 scripts/test-update-selection-flow-fix.py

      - name: Test two-step update flow
        run: python3 scripts/test-two-step-update-flow.py

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Test local engine
        run: sh engine/run-tests.sh

      - name: Recover proven APK Builder host overlay
        run: |
          git show 9aab10bbbf3d11e0f1b9521896256f833b6c828c:overlay/apk-builder.patch > /tmp/apk-builder.patch
          git show 9aab10bbbf3d11e0f1b9521896256f833b6c828c:scripts/apply-native-features.py > /tmp/apply-native-features.py
          cp overlay/hide-browser-toolbar.patch /tmp/hide-browser-toolbar.patch

      - name: Clone pinned Android host source
        run: |
          git clone https://github.com/shiaho777/web-to-app.git source
          git -C source checkout 8870ee293dbb89f5293c49c3f25e767f3a996e1c
          git -C source apply --check /tmp/apk-builder.patch
          git -C source apply /tmp/apk-builder.patch
          git -C source apply --check /tmp/hide-browser-toolbar.patch
          git -C source apply /tmp/hide-browser-toolbar.patch
          cp /tmp/apply-native-features.py source/scripts/apply-native-features.py
          python3 source/scripts/apply-native-features.py
          grep -F 'val hideBrowserToolbar: Boolean = true' source/app/src/main/java/com/webtoapp/data/model/WebApp.kt
          grep -F 'val toolbarShowTitle: Boolean = false' source/app/src/main/java/com/webtoapp/data/model/WebApp.kt
          grep -F 'val browserToolbarCustomized: Boolean = true' source/app/src/main/java/com/webtoapp/data/model/WebApp.kt
          test -f source/app/src/test/java/com/webtoapp/data/model/ApkBuilderGeneratedAppDefaultsTest.kt

      - name: Inject safe two-step update and Save APK features
        run: |
          mkdir -p source/app/src/main/java/com/osulsa/apkbuilder/engine
          cp engine/src/main/java/com/osulsa/apkbuilder/engine/*.java source/app/src/main/java/com/osulsa/apkbuilder/engine/
          rm -f source/app/src/main/java/com/osulsa/apkbuilder/engine/LocalZipBuildEngine.java.tmp
          rm -f source/app/src/main/java/com/osulsa/apkbuilder/engine/LocalZipBuildEngine2.java
          rm -f source/app/src/main/java/com/osulsa/apkbuilder/engine/LocalZipBuildEngine3.java
          python3 scripts/fix-existing-apk-update-script.py scripts/apply-existing-apk-update.py
          python3 scripts/apply-existing-apk-update.py source
          python3 scripts/apply-update-selection-flow-fix.py source
          python3 scripts/apply-two-step-update-flow.py source
          python3 scripts/apply-save-apk-action.py source
          grep -F 'var updateExistingSelected by remember { mutableStateOf(false) }' source/app/src/main/java/com/webtoapp/ui/wizard/ApkBuilderWizard.kt
          grep -F 'val sourceReady = updateExistingSelected || when (source)' source/app/src/main/java/com/webtoapp/ui/wizard/ApkBuilderWizard.kt
          grep -F 'Text("Step 1 of 2")' source/app/src/main/java/com/webtoapp/ui/wizard/UpdateExistingApkDialog.kt
          grep -F 'Text("Step 2 of 2")' source/app/src/main/java/com/webtoapp/ui/wizard/UpdateExistingApkDialog.kt
          grep -F 'ProjectArchive.prepare' source/app/src/main/java/com/webtoapp/ui/wizard/UpdateExistingApkDialog.kt
          ! grep -F 'ZipProjectImporter.importZip' source/app/src/main/java/com/webtoapp/ui/wizard/UpdateExistingApkDialog.kt
          grep -F 'ExistingApkWebUpdater.prepareUnsigned' source/app/src/main/java/com/webtoapp/ui/wizard/UpdateExistingApkDialog.kt
          grep -F 'Text("Save APK"' source/app/src/main/java/com/webtoapp/ui/screens/BuildApkScreen.kt
          test -f source/app/src/test/java/com/webtoapp/core/apkbuilder/ApkFileSaverTest.kt
          grep -F 'versionCode = 520' source/app/build.gradle.kts
          grep -F 'versionName = "5.2.0"' source/app/build.gradle.kts

      - name: Install Android build tools
        run: sdkmanager --install 'platform-tools' 'platforms;android-36' 'build-tools;36.0.0' 'cmake;3.22.1' 'ndk;28.2.13676358'

      - name: Run Android unit tests
        working-directory: source
        run: ./gradlew --no-daemon --no-configuration-cache -Dkotlin.compiler.execution.strategy=in-process -PskipShellTemplateSync=true -PallowDebugSignedRelease=true :app:testStandardDebugUnitTest :app:checkConfigFieldDrift

      - name: Build Android host APK
        working-directory: source
        run: ./gradlew --no-daemon --no-configuration-cache -Dkotlin.compiler.execution.strategy=in-process -PallowDebugSignedRelease=true :app:assembleStandardDebug

      - name: Verify host and embedded shell
        run: |
          APK=$(find source/app/build/outputs/apk -name '*.apk' -type f | head -1)
          test -s "$APK"
          unzip -tqq "$APK"
          unzip -Z1 "$APK" | grep -Fx 'assets/template/webview_shell.apk'
          unzip -p "$APK" assets/template/webview_shell.apk > webview_shell.apk
          test -s webview_shell.apk
          unzip -tqq webview_shell.apk
          "$ANDROID_HOME/build-tools/36.0.0/apksigner" verify --verbose --print-certs "$APK"
          "$ANDROID_HOME/build-tools/36.0.0/aapt" dump badging "$APK" | grep -F "package: name='com.locon007.apkbuilder' versionCode='520' versionName='5.2.0'"
          grep -F 'ProjectArchive.prepare' source/app/src/main/java/com/webtoapp/ui/wizard/UpdateExistingApkDialog.kt
          grep -F 'enabled = !busy && existingInfo != null && project != null' source/app/src/main/java/com/webtoapp/ui/wizard/UpdateExistingApkDialog.kt
          grep -F 'ApkFileSaver.copyApk(apkFile, it)' source/app/src/main/java/com/webtoapp/ui/screens/BuildApkScreen.kt
          grep -F 'failures="0"' source/app/build/test-results/testStandardDebugUnitTest/TEST-com.webtoapp.core.apkbuilder.ApkFileSaverTest.xml
          cp "$APK" APK-Builder-5.2.0-debug.apk
          sha256sum APK-Builder-5.2.0-debug.apk > APK-Builder-5.2.0-debug.apk.sha256
          mkdir -p signing-tools
          cp "$ANDROID_HOME/build-tools/36.0.0/lib/apksigner.jar" signing-tools/apksigner.jar
          printf 'source_branch=%s\nsource_commit=%s\n' "$GITHUB_REF_NAME" "$GITHUB_SHA" > BUILD_EVIDENCE.txt
          printf 'toolbar_regression_fix=PASS\nsave_apk_action=PASS\nupdate_source_selection_fix=PASS\ntwo_step_update_flow=PASS\nhardened_project_archive=PASS\n' >> BUILD_EVIDENCE.txt
          cat APK-Builder-5.2.0-debug.apk.sha256 >> BUILD_EVIDENCE.txt

      - name: Upload one-time build artifact
        uses: actions/upload-artifact@v4
        with:
          name: APK-Builder-5.2.0-audited-host
          path: |
            APK-Builder-5.2.0-debug.apk
            APK-Builder-5.2.0-debug.apk.sha256
            BUILD_EVIDENCE.txt
            signing-tools/apksigner.jar
          if-no-files-found: error
          retention-days: 7
